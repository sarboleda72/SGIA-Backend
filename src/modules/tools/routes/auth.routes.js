const Router = require('express');

// API middlewares
const { } = require('../api/auth.api');

// Inicializar router

// Rutas post

// Rutas get

// Rutas put

// Rutas delete

module.exports = router;
